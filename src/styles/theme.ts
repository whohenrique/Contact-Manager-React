import styled from "styled-components";

export const Wrapper = styled.section`
    padding: 4rem;
    background: blue;
    text-align: center;

`;

export const Title = styled.h1`
    font-size: 54;
    color: white;
`;